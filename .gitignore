node_modules/
dist/

# Bitfocus modules use yarn; a npm lockfile fails their CI
package-lock.json

# yarn (nodeLinker: node-modules, so yarn.lock IS committed)
.yarn/*
!.yarn/patches
.pnp.*

# output of `yarn package`
pkg/
pkg.tgz
*.tgz
.DS_Store

# SMB/AppleDouble metadata from network shares
.*00030000*
